package it.objectmethod.springboot.dto.mapper;

public class CityMapper
//extends Mapper<City, CityDTO> 
{

	//	@Autowired
	//	private CityRepository cityRepository;
	//
	//	@Override
	//	public City toEntity(CityDTO dto) {
	//		City city = null;
	//		if (dto != null) {
	//			if (dto.getId() != null) {
	//				city = cityRepository.findOne(dto.getId());
	//			} else {
	//				city = new City();
	//			}
	//			city.setName(dto.getName());
	//			city.setPopulation(dto.getPopulation());
	//			city.setCountryId(dto.getCountryId());
	//		}
	//		return city;
	//	}
	//
	//	@Override
	//	public CityDTO toDto(City entity) {
	//		CityDTO city = null;
	//		if (entity != null) {
	//			city = new CityDTO();
	//			city.setId(entity.getId());
	//			city.setName(entity.getName());
	//			city.setPopulation(entity.getPopulation());
	//			city.setCountryId(entity.getCountryId());
	//		}
	//		return city;
	//	}

}
